Emmet is an essential toolkit and plugin for text editors that lets web developers write HTML and CSS code much faster using short CSS-like abbreviations
A Content Delivery Network (CDN) is a group of connected servers spread across the world that work together to load web pages and internet content faster.
When we use React CDN links wrapped in the script tags, we have React injected inside it.
A react element at the end of the day is an element.
We are going to use webpack bundler for Namaste React.
We will be using "jest" for testing.
